{
  "schema_version": "ts-decision",
  "action": "end_node",
  "rationale": "Close endpoint validation before proposing a mechanism hypothesis.",
  "evidence_refs": [
    "${EV_ENDPOINT_PROVENANCE}",
    "${EV_CHARGE_MULTIPLICITY}",
    "${EV_ATOM_MAPPING}",
    "${EV_REACTION_CENTER_DELTA}"
  ],
  "report_ref": {
    "report_id": "${REPORT_ID}",
    "workspace_root": "${WORKSPACE_ROOT}"
  },
  "payload": {
    "node_id": "n000",
    "closure": {
      "program_status": "completed",
      "claim_verdict": "supported",
      "program": {
        "summary": "Endpoint provenance, charge/multiplicity, atom mapping, and reaction-center deltas were checked.",
        "evidence_refs": [
          "${EV_ENDPOINT_PROVENANCE}",
          "${EV_CHARGE_MULTIPLICITY}",
          "${EV_ATOM_MAPPING}",
          "${EV_REACTION_CENTER_DELTA}"
        ]
      },
      "mechanism": {
        "summary": "The endpoint facts are sufficient to formulate an explicit mechanism hypothesis, but no mechanism has been proposed or tested yet.",
        "evidence_refs": []
      },
      "implication": "Use propose_hypothesis before opening a hypothesis-referenced evidence node.",
      "open_questions": []
    }
  }
}
