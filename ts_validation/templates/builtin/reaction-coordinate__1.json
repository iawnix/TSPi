{
  "schema_version": "ts-gate-template/1",
  "template_id": "reaction-coordinate",
  "version": "1",
  "title": "Imaginary mode assignment",
  "parameters": {
    "subject_ref": {"type": "string", "required": true}
  },
  "definition": {
    "checks": [
      {"check_id": "mode_assignment", "predicate": "observation.equals", "parameters": {"selector": {"concept_id": "vibration.mode_matches_reaction_coordinate", "subject_ref": "${subject_ref}"}, "expected": true}, "blocking": true}
    ],
    "success_policy": {"mode": "all_blocking"}
  }
}
