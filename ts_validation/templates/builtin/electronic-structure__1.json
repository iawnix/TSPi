{
  "schema_version": "ts-gate-template/1",
  "template_id": "electronic-structure",
  "version": "1",
  "title": "Electronic-structure suitability",
  "parameters": {
    "subject_ref": {"type": "string", "required": true}
  },
  "definition": {
    "checks": [
      {"check_id": "normal_termination", "predicate": "observation.equals", "parameters": {"selector": {"concept_id": "program.normal_termination", "subject_ref": "${subject_ref}"}, "expected": true}, "blocking": true},
      {"check_id": "electronic_structure", "predicate": "observation.equals", "parameters": {"selector": {"concept_id": "electronic_structure.assignment_supported", "subject_ref": "${subject_ref}"}, "expected": true}, "blocking": true}
    ],
    "success_policy": {"mode": "all_blocking"}
  }
}
