{
  "schema_version": "ts-artifact-manifest/2",
  "node_id": "${NODE_ID}",
  "node_type": "${NODE_TYPE}",
  "claim_layer": "${NODE_TYPE}",
  "cross_node_consumption_reason": "This node validates or audits artifacts generated by an earlier node without moving those source files.",
  "evidence_refs": [
    "${EVIDENCE_ID}"
  ],
  "owned_artifacts": [
    {
      "path": "nodes/${NODE_ID}/outputs/${OWNED_ARTIFACT}",
      "purpose": "Primary artifact produced by this node and cited by this node's evidence.",
      "evidence_id": "${EVIDENCE_ID}"
    }
  ],
  "consumed_artifacts": [
    {
      "path": "nodes/${SOURCE_NODE_ID}/outputs/${SOURCE_ARTIFACT}",
      "source_node": "${SOURCE_NODE_ID}",
      "purpose": "Upstream artifact consumed as input or source material for this node."
    }
  ]
}
