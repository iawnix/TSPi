{
  "action": "update_workspace",
  "rationale": "Evaluate a declared scientific Gate from active evidence facts.",
  "basisRefs": ["${EVIDENCE_ID}"],
  "payload": {
    "evaluate_gate": {
      "node_id": "${NODE_ID}",
      "gate": "${GATE_TYPE}",
      "evidence_refs": ["${EVIDENCE_ID}"],
      "target_ref": "${CLAIM_ID}"
    }
  }
}
