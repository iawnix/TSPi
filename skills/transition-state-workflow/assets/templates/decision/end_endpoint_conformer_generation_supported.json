{
  "schema_version": "ts-decision",
  "action": "end_node",
  "rationale": "Close the conformer strategy after selecting usable R/P representatives without promoting them to TS evidence.",
  "evidence_refs": [
    "${EV_ENDPOINT_CONFORMER_ENSEMBLE}",
    "${EV_SELECTED_ENDPOINT_CONFORMER}",
    "${EV_ENDPOINT_IDENTITY_GATE}",
    "${EV_ENDPOINT_MINIMUM_GATE}"
  ],
  "report_ref": {
    "report_id": "${REPORT_ID}",
    "workspace_root": "${WORKSPACE_ROOT}"
  },
  "payload": {
    "node_id": "${CONFORMER_NODE_ID}",
    "closure": {
      "program_status": "completed",
      "claim_verdict": "supported",
      "program": {
        "summary": "R/P conformer generation, identity filtering, selection, and minimum checks completed.",
        "evidence_refs": [
          "${EV_ENDPOINT_CONFORMER_ENSEMBLE}",
          "${EV_SELECTED_ENDPOINT_CONFORMER}",
          "${EV_ENDPOINT_IDENTITY_GATE}",
          "${EV_ENDPOINT_MINIMUM_GATE}"
        ]
      },
      "mechanism": {
        "summary": "The selected structures are usable endpoint representatives only; no TS or connectivity claim was tested.",
        "hypothesis_ref": {
          "hypothesis_id": "${HYPOTHESIS_ID}",
          "prediction_ids": []
        },
        "impact_scope": "solution_only",
        "evidence_refs": [
          "${EV_ENDPOINT_CONFORMER_ENSEMBLE}",
          "${EV_SELECTED_ENDPOINT_CONFORMER}",
          "${EV_ENDPOINT_IDENTITY_GATE}",
          "${EV_ENDPOINT_MINIMUM_GATE}"
        ]
      },
      "implication": "Use selected endpoint representatives in a separate TS-candidate generation strategy.",
      "open_questions": []
    }
  }
}
