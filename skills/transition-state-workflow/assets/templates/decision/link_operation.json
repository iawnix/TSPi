{
  "action": "update_workspace",
  "rationale": "Bind an immutable operation record to its owning node.",
  "basisRefs": ["${OPERATION_REF}"],
  "payload": {
    "link_operation": {
      "node_id": "${NODE_ID}",
      "operation_ref": "${OPERATION_REF}"
    }
  }
}
