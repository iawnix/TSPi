{
  "schema_version": "ts-decision",
  "action": "start_node",
  "rationale": "Start candidate generation for the active mechanism hypothesis.",
  "evidence_refs": [
    "${EV_REACTION_CENTER_DELTA}"
  ],
  "report_ref": {
    "report_id": "${REPORT_ID}",
    "workspace_root": "${WORKSPACE_ROOT}"
  },
  "payload": {
    "node_id": "${NODE_ID}",
    "parent_node": "n000",
    "phase": "candidate_generation",
    "hypothesis": "Generate a TS candidate consistent with the active reaction-center hypothesis.",
    "hypothesis_ref": {
      "hypothesis_id": "${HYPOTHESIS_ID}",
      "prediction_ids": [
        "${PRED_CANDIDATE_ID}"
      ]
    },
    "solution_ref": {
      "solution_id": "${SOLUTION_ID}",
      "strategy": "${SOLUTION_STRATEGY}",
      "summary": "${SOLUTION_SUMMARY}"
    },
    "branch_context": {
      "relation": "continue_parent",
      "from_node": "n000",
      "anchor_node": "n000"
    },
    "pathway_ref": {
      "pathway_id": "${PATHWAY_ID}",
      "step_id": "${STEP_ID}"
    },
    "expected_evidence": [
      "candidate_geometry",
      "candidate_generation_log"
    ]
  }
}
