{
  "action": "start_node",
  "rationale": "Open one bounded research act selected by the Root Agent.",
  "basisRefs": ["${BASIS_REF}"],
  "payload": {
    "node_id": "${NODE_ID}",
    "parent_node": "${PARENT_NODE}",
    "objective": "${OBJECTIVE}",
    "tags": ["${TAG}"],
    "claim_refs": ["${CLAIM_ID}"]
  }
}
