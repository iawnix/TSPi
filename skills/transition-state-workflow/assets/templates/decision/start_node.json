{
  "schema_version": "ts-decision/3",
  "decision_id": "${DECISION_ID}",
  "action": "start_node",
  "rationale": "Open one bounded research act selected by the Root Agent.",
  "basis_refs": ["${BASIS_REF}"],
  "report_ref": {
    "report_id": "${REPORT_ID}",
    "workspace_root": "${WORKSPACE_ROOT}"
  },
  "base_revision": "${BASE_REVISION}",
  "payload": {
    "node_id": "${NODE_ID}",
    "parent_node": "${PARENT_NODE}",
    "objective": "${OBJECTIVE}",
    "tags": ["${TAG}"],
    "claim_refs": ["${CLAIM_ID}"]
  }
}
