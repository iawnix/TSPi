{
  "schema_version": "ts-decision/2",
  "decision_id": "${DECISION_ID}",
  "action": "start_node",
  "rationale": "Validate one declared hypothesis prediction.",
  "evidence_refs": [],
  "base_revision": "${BASE_REVISION}",
  "report_ref": {"report_id": "${REPORT_ID}", "workspace_root": "${WORKSPACE_ROOT}"},
  "payload": {
    "node_id": "${NODE_ID}",
    "parent_node": "${PARENT_NODE}",
    "node_type": "validation",
    "validation_scope": "tsfreq",
    "objective": "Test the declared TS/Freq prediction.",
    "hypothesis_ref": {"hypothesis_id": "${HYPOTHESIS_ID}", "prediction_ids": ["${PREDICTION_ID}"]},
    "branch_context": {"relation": "continue_parent", "from_node": "${PARENT_NODE}", "anchor_node": "n000"},
    "expected_evidence": ["tsfreq_gate"]
  }
}
