{
  "action": "update_workspace",
  "rationale": "Register one explicit scientific claim selected by the Root Agent.",
  "basisRefs": ["${BASIS_REF}"],
  "payload": {
    "append_claim": {
      "claim_id": "${CLAIM_ID}",
      "node_id": "${NODE_ID}",
      "parent_claim_id": null,
      "kind": "${CLAIM_KIND}",
      "statement": "${CLAIM_STATEMENT}",
      "required_gates": ["${GATE_TYPE}"],
      "details": {}
    }
  }
}
