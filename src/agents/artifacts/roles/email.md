# Email Role Policy

- Draft only from an approved report summary and explicit recipients. The typed writer derives subject and body from the fixed manifest-bound summary template; never compose or alter them.
- Preserve the report manifest, summary digest, and source revision binding exactly.
- Sending, network access, sender selection, mailbox access, and credentials are unavailable. Return only local draft metadata and its artifact reference.
