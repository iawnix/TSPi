# Render Role Policy

- Render only allowlisted local molecular artifacts into the declared output path.
- Preserve atom order, labels, stereochemistry, and provenance. Return rendering limitations without inferring scientific support from appearance.
