"""ASE-managed NEB candidate-generation toolkit.

Split from the former single-file ``ase_neb_framework`` module into cohesive
layers: configuration, Gaussian force calculation, geometry heuristics, workspace
persistence, the NEB driver, validation planning, and the command-line interface.
"""
